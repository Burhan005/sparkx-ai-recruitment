/**
 * mockData.js — INTENTIONALLY EMPTY
 *
 * All data is now served exclusively from the FastAPI backend (SQLite/PostgreSQL).
 * There is NO static/hardcoded data in this application.
 *
 * Backend endpoints:
 *   GET /api/jobs           — All job postings
 *   GET /api/candidates     — All candidates
 *   GET /api/presets        — Resume demo presets
 *   GET /api/health         — DB health + record counts
 *
 * To populate the database run:
 *   cd backend && python seed.py
 */

// This file is intentionally empty. Do not add static data here.
export {};